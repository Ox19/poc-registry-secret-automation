# /cargado: confirma que existe el secret de GitHub del pedido (solo nombre y fecha, nunca el valor)
# y avisa al analista que ya puede aprobar.
name: "Confirmar carga del valor"
run-name: "Confirmar carga — #${{ github.event.issue.number }}"

on:
  issue_comment:
    types: [created]

# Grupo propio: no puede compartir el de "Registrar secreto", o cancelaría la corrida que espera aprobación.
concurrency:
  group: cargado-${{ github.event.issue.number }}

permissions: {}

jobs:
  confirm:
    name: Confirmar carga
    if: >-
      !github.event.issue.pull_request && github.event.issue.state == 'open' &&
      contains(github.event.issue.labels.*.name, 'registro-secreto') &&
      !contains(github.event.issue.labels.*.name, 'con-errores') &&
      startsWith(github.event.comment.body, '/cargado')
    environment: github-app
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read  # checkout de los scripts
      issues: write   # actualizar el comentario del bot
      actions: read   # encontrar la corrida que espera aprobación
    steps:
      - name: Token de la GitHub App (solo lectura de secrets de GitHub)
        id: app
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1  # v3.2.0
        with:
          client-id: ${{ vars.APP_CLIENT_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          permission-secrets: read

      - name: Buscar el secret de GitHub del pedido
        id: find
        uses: actions/github-script@f28e40c7f34bde8b3046d885e986cb6290c5673b  # v7
        with:
          github-token: ${{ steps.app.outputs.token }}
          script: |
            const secret = { owner: context.repo.owner, repo: context.repo.repo, secret_name: `VALOR_ISSUE_${context.issue.number}` };
            try {
              const { data } = await github.rest.actions.getRepoSecret(secret);
              core.setOutput('found', 'true');
              core.setOutput('updated_at', data.updated_at);
            } catch (error) {
              if (error.status !== 404) throw error;
              core.setOutput('found', 'false');
            }

      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4
        with:
          persist-credentials: false

      - name: Contarlo en el issue
        uses: actions/github-script@f28e40c7f34bde8b3046d885e986cb6290c5673b  # v7
        env:
          FOUND: ${{ steps.find.outputs.found }}
          UPDATED_AT: ${{ steps.find.outputs.updated_at }}
          SECURITY_TEAM: ${{ vars.SECURITY_TEAM }}
        with:
          script: await require('./.github/scripts/confirmar-carga.js')({ github, context })
